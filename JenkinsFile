pipeline {
    agent any

    environment {
        REPO_DIR = "bts_repo_ex"
        API_URL = "http://192.168.2.141:8088/bts/jenkins/getCommits"
    }

    stages {
        stage("Init") {
            steps {
                sh '''
                    if [ ! -d "${REPO_DIR}/.git" ]; then
                        echo "⚠️ Git 저장소 없음 — 새로 clone 합니다."
                        git clone https://github.com/lsb06111/bts_repo_ex.git
                    else
                        echo "✅ 기존 저장소 존재"
                    fi
                '''
            }
        }

        stage("Selective Pull") {
            steps {
                dir("${REPO_DIR}") {
                    sh '''
                        echo "📡 커밋 정보 API 호출 중..."
                        curl -s ${API_URL} -o commits.json

                        echo "📁 커밋 파일 목록:"
                        cat commits.json

                        git fetch origin main

                        # jq 사용: 각 커밋의 sha, fileName 추출
                        jq -c '.[]' commits.json | while read -r item; do
                            SHA=$(echo $item | jq -r '.sha')
                            FILE=$(echo $item | jq -r '.fileName')

                            echo "=============================================="
                            echo "SHA: $SHA"
                            echo "File: $FILE"
                            echo "----------------------------------------------"

                            # 파일이 존재하는 커밋인지 확인
                            if git cat-file -e ${SHA} 2>/dev/null; then
                                echo "✅ 커밋 존재 — 해당 파일 checkout"
                                git checkout ${SHA} -- "${FILE}"
                            else
                                echo "⚠️ 커밋 ${SHA} 존재하지 않음 — 스킵"
                            fi
                        done
                    '''
                }
            }
        }

        stage("Permission") {
            steps {
                dir("${REPO_DIR}") {
                    sh 'chmod u+x mvnw'
                }
            }
        }

        stage("Clean") {
            steps {
                dir("${REPO_DIR}") {
                    sh './mvnw clean'
                }
            }
        }

        stage("Build") {
            steps {
                dir("${REPO_DIR}") {
                    sh './mvnw package -DskipTests'
                }
            }
        }

        stage("Server start") {
            steps {
                dir("${REPO_DIR}") {
                    sh '''
                        #!/bin/bash
                        set -e
                        
                        if [ -f app.pid ]; then
                            PID=$(cat app.pid)
                            if kill -0 $PID 2>/dev/null; then
                                echo "기존 프로세스 종료: PID $PID"
                                kill $PID
                            fi
                            rm app.pid
                        fi

                        nohup java -jar target/*.jar > app.log 2>&1 & echo $! > app.pid

                        echo "서버 기동 대기 중..."
                        for i in $(seq 1 15); do
                            sleep 2
                            if curl -s -f http://localhost:8081/actuator/health | grep -q '"status":"UP"'; then
                                echo "✅ 서버 기동 성공"
                                exit 0
                            fi
                        done
                        echo "❌ 서버 기동 실패"
                        exit 1
                    '''
                }
            }
        }
    }
}